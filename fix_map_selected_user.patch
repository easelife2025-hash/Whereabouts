--- app/(main)/map/page.tsx
+++ app/(main)/map/page.tsx
@@ -48,6 +48,10 @@
             if (prev && !newMarkersMap.has(prev.uid)) {
               return null;
             }
+            // Update selected user with new location data if it's currently selected
+            if (prev && newMarkersMap.has(prev.uid)) {
+              return newMarkersMap.get(prev.uid);
+            }
             return prev;
           });
         }, (error) => {
